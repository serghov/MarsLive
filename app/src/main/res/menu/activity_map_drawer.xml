<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:checked="true"
            android:id="@+id/nav_elevation"
            android:icon="@drawable/ic_action_elevation_icon"
            android:title="Elevation"/>
        <item
            android:id="@+id/nav_visible"
            android:icon="@drawable/ic_action_visible_icon"
            android:title="Visible"/>
        <item
            android:id="@+id/nav_infrared"
            android:icon="@drawable/ic_action_infrared_icon"
            android:title="Infrared"/>
    </group>

    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="Share"/>
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Send"/>
        </menu>
    </item>

</menu>
