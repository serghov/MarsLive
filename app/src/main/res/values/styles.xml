<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">#273238</item>

        <item name="android:actionMenuTextColor">@color/drawerTextColor</item>
        <item name="android:itemTextAppearance">@color/drawerTextColor</item>
        <item name="android:textColor">@color/drawerTextColor</item>

        <item name="android:listDivider">@color/drawerTextColor</item>


    </style>

    <style name="TextAppearance.Widget.IconMenu.Item" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textColor">@color/drawerTextColor</item>
    </style>


    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark"/>


</resources>
